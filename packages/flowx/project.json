{
  "name": "@async-kit/flowx",
  "sourceRoot": "packages/flowx/src",
  "projectType": "library",
  "tags": ["scope:flowx", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "../../node_modules/.bin/tsup",
        "cwd": "packages/flowx"
      },
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "../../node_modules/.bin/tsc -p tsconfig.lib.json --noEmit",
        "cwd": "packages/flowx"
      },
      "inputs": ["default", "^production"],
      "cache": true
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm publish --access public",
        "cwd": "packages/flowx"
      },
      "dependsOn": ["build"]
    }
  }
}
