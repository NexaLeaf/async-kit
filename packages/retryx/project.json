{
  "name": "@async-kit/retryx",
  "sourceRoot": "packages/retryx/src",
  "projectType": "library",
  "tags": ["scope:retryx", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "../../node_modules/.bin/tsup",
        "cwd": "packages/retryx"
      },
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "../../node_modules/.bin/tsc -p tsconfig.lib.json --noEmit",
        "cwd": "packages/retryx"
      },
      "inputs": ["default", "^production"],
      "cache": true
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm publish --access public",
        "cwd": "packages/retryx"
      },
      "dependsOn": ["build"]
    }
  }
}
