{
  "name": "@async-kit/cachex",
  "sourceRoot": "packages/cachex/src",
  "projectType": "library",
  "tags": ["scope:cachex", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "../../node_modules/.bin/tsup", "cwd": "packages/cachex" },
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "../../node_modules/.bin/tsc -p tsconfig.lib.json --noEmit", "cwd": "packages/cachex" },
      "inputs": ["default", "^production"],
      "cache": true
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": { "command": "npm publish --access public", "cwd": "packages/cachex" },
      "dependsOn": ["build"]
    }
  }
}
