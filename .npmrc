# Default npm registry
registry=https://registry.npmjs.org/

# Enable provenance for supply chain security (requires npm >=9.5 and OIDC in CI)
provenance=true

# Scoped packages default to public access
@async-kit:registry=https://registry.npmjs.org/
